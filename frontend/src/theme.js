import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    amazonOrange: '#FF9900', // Amazon's orange color
    amazonBlue: '#0079ad',
  },
});

export default theme;
